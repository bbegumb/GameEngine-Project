#pragma once

#include <memory>

class Scene;
class ShaderProgram;

namespace DemoScene {
    void createScene1(Scene& scene);
    void createScene2(Scene& scene);
    void createScene3(Scene& scene);
}